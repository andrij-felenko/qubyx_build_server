import './Header.css';

const Header = () => {
    return (
        <header className='header'>
            <h1>Build Server</h1>
            <p>You can build any Qubyx product here!</p>
        </header>
    );
};

export default Header;